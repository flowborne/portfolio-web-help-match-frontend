
import { SxProps, Theme } from '@mui/system';

export const styles: { [key: string]: SxProps<Theme> } = {
  container: {
    width: '100%',
    padding: '16px 32px',
    bgcolor: '#fff',
    borderRadius: '8px',
  },
  
};
